# Resume Point — 2026-05-21 PM

**If you lose this session, this file is the canonical pick-up point.**
**Magic phrase to resume:** `pawconscious-resume-21may`

Deadline: 2026-06-05 noon PT. Today is Day 17 of 19. 15 calendar days remaining.

---

## State of the system right now

### What is working

- **Cloud Run service `mesh-api` live** at `https://mesh-api-oasa5mxega-uc.a.run.app` (revision `mesh-api-00044-4lc`).
- **A2A v0.3 dual-shape envelope** accepts both flat `{skill, input}` and standard `params.message.parts` (shipped rev 41, v0.11.0).
- **Demo key** `demo-key-2026-06` in the 401 message body so judges see it (rev 41).
- **`/agents` Stripe-style A2A reference** page (v0.12.0).
- **`/demo/shopper` live page** — vanilla JS shopper agent calls mesh A2A round-trip (v0.12.1).
- **ADK eval re-run** — 3/3 PASS on eligible cases, 17/20 skipped on dead URLs (v0.12.2).
- **Vertex AI Search corpus** — `acp-regulator-corpus` in `global` region, 7 documents indexed (5 FTC §255 + AAFCO PF7 + NASC seal summaries). Live verify_claim against `nativepet.com/hip-joint` confirms grounding_sources are populated.
- **Reasoning Engine deployed** — `projects/40952019806/locations/us-central1/reasoningEngines/1255381144908595200` (display name "PawConscious Mesh — ACP for Pet (orchestrator)"). **Not in traffic path.**
- **Firestore transparency log** — chain anchor + URN resolution working at `/pcec/v0/claim/{urn}` and `/pcec/v0/chain/head`.
- **Ed25519 signer** — key in GCP Secret Manager, real signatures on every bundle.

### What is broken or aspirational

| # | Issue | Severity | Source |
|---|---|---|---|
| 1.3 | Agent Engine deployed but Cloud Run runs mesh inline via `asyncio.gather`; Reasoning Engine `query` is never invoked. Health probe and agent card overclaim. | P0 | codex-verified |
| 1.4 | "Mesh" is one Python process. Only `claim_extractor.py` (1/7 agents) uses ADK `LlmAgent`. Orchestrator uses `asyncio.gather`, not `ParallelAgent`. | P0 | codex-verified |
| N1 | Agent card advertises `fetch_substantiation_bundle` skill; only `verify_claim` is routed. | P0 | codex-caught |
| N3a | Second Opinion returned `overall_verdict: "CONFIRMS"` on parse failure (adversarial agent fails OPEN). **FIXED 2026-05-21 PM** — now returns `"UNAVAILABLE"`. Not deployed yet. | P0 | shipped to code, awaiting deploy |
| N3b | Firestore append failure logs error but UI says "appended." | P1 | codex-caught |
| N3c | Multiple `except Exception` blocks fail silently across agents/ tree. | P1 | codex-caught |
| N2 | `max_claims=1` (eval) vs `max_claims=3` (UI hardcoded) divergence. | P1 | codex-caught |
| N4 | `/agents` page hardcodes a base URL different from `PUBLIC_BASE_URL`. | P1 | codex-caught |
| Eval | 17/20 cases skipped on dead PDP URLs. Optically reads as cherry-picked even though methodology is honest. | P1 | audit |
| UX | Primary nav inconsistent across `/`, `/agents`, `/architecture`, `/demo/shopper` (three of four have different navs or none). | P1 | UX audit |
| UX | Buyer + Developer hero h1 are nearly identical lines. | P1 | UX audit |
| UX | 8 sections on landing page; 4 of them (proof + moats + founder + track3) collapse conceptually. | P1 | UX audit |

### What was an overclaim I retracted

- **1.2 Vertex Search corpus empty.** I was wrong. The data store exists in `global` region (I probed `us` earlier). 7 documents indexed. Live retrieval returns real grounding_sources. Compliance grounding is genuine in production. See PRODUCTION_REVIEW_2026-05-21.md Section 7 for the full correction.

---

## Locked decisions (Omar approved 2026-05-21 PM)

1. **R3 ADK scope: 4/7 agents on ADK.** Orchestrator (SequentialAgent + ParallelAgent) + claim_extractor (LlmAgent + Firecrawl FunctionTool) + evidence_grader (LlmAgent + BioMCP FunctionTool) + compliance (LlmAgent + Vertex AI Search FunctionTool) + auditor (LlmAgent). Vet_rubric, report_writer, second_opinion stay direct genai.
2. **Corpus: FTC ingested + AAFCO/NASC as public-side summaries (already in corpus/).** No external URL fetch — they're already written as derivative summaries in the project's own voice. No licensing risk for the public repo.
3. **`fetch_substantiation_bundle`: WIRE IT (not drop).** Route the skill to the existing `/pcec/v0/claim/{urn}` Firestore-backed logic. ~2 hours.
4. **Order: N3a tonight (DONE 2026-05-21 PM), R1 next.** R1 is now ~1 hour (verification only, corpus already exists).
5. **Primary nav label: "Product"** (not "Buyers"). 4-item nav: Product · Developers · Architecture · Demo.
6. **Mobile QA cut** — desktop-only judging accepted, mobile is Q3 work.

---

## The 15-day plan (post-codex amendments + R1 budget recovery)

### Today (Day 17, 2026-05-21 PM) — done

- ✅ N3a Second Opinion fail-CLOSED (`agents/second_opinion.py:125-135` + console-v2.html badges). Codex CLEAR (STRONG). Shipped rev `mesh-api-00045-hcw` commit `ff50fbd`.
- ✅ R1 verification — corpus exists, retrieval works in production. Budget reclaimed.

### Day 18 (2026-05-21 PM, pulled forward) — done in one batch

- ✅ **R1 finish.** `compliance.py:86` default `max_results` 3 → 5. Added `GET /health/vertex-search` live probe (returns ok/empty/error + data store path + sources count + 3-sample). Architecture page rewrites Stage 1 + Stage 3 copy to match current state (honest "single-process multi-agent pipeline with public A2A mesh at the edge" instead of overclaim).
- ✅ **N1 fetch_substantiation_bundle wired.** `A2ATaskRequest.resolve_url_and_skill` now accepts URN slot via 4 shapes (flat input.urn/bundle_urn, json part data.urn/bundle_urn, text part starting with urn:pcec:claim:, params wrap). `a2a_send` routes both skills. Read-only via `fetch_bundle_async`, returns 202 with status=completed and estimated_seconds=0. Codex P1 amendment absorbed — URN-shaped tokens only accepted when `skill == fetch_substantiation_bundle`; verify_claim still requires http(s).
- ✅ **U1 global nav.** `_GLOBAL_NAV_TEMPLATE` + `_global_nav(active)` helper. 4 items: Product · Developers · Architecture · Demo. Sticky, backdrop-blur, JetBrains Mono labels, active state = electric cyan. Same on every page via `_render_page(file, active)` placeholder substitution.
- ✅ **U2 global footer.** 5 columns: Product · Developers · Trust · Company · Legal. Trust column exposes DID, Chain head, Vertex Search health, Agent Engine health. Same on every page.
- ✅ **Codex handshake** — Day 18 batch absorbed P1 (URN gating). Re-checked STRONG.

### Day 19 (2026-05-22) — 1.5 days (next)
- **R2 Reasoning Engine wiring** (1 day): feature flag `ACP_USE_AGENT_ENGINE=true/false`. When true, `_run_verify_claim_background` calls `reasoning_engine.query()` against the deployed pickle. Cold-start p95 gate: if Agent Engine p95 > 2× inline p95, auto-fallback. Inline stays primary; Agent Engine is the documented Track 3 path.
- **U3 hero divergence** (0.5 day): shared h1 ("The trust layer AI agents call before recommending a consumer product"), divergent subheads. Product subhead = buyer-facing. Developers subhead = integrator-facing speed-first.
- **Codex handshake** before EOD.

### Day 20 (2026-05-24) — 2 days
- **R3 ADK migration day 1** (1 day): orchestrator → `SequentialAgent` + `ParallelAgent`. `evidence_grader` → `LlmAgent` + `FunctionTool` wrapping `search_pubmed`. `auditor` → `LlmAgent`.
- **U4 landing collapse** (1 day): 8 sections → 5. Merge `#how` + `#architecture` → "How it works". Merge `#proof` + `.moats` + `.founder` + `.track3` → "Why trust this" with 4 sub-cards. Keep `#hero`, `#result`, `#biz`.
- **Codex handshake** before EOD.

### Day 21 (2026-05-25) — 1.5 days
- **R3 ADK migration day 2** (1 day): `compliance` → `LlmAgent` + `FunctionTool` wrapping `retrieve_grounding_sources`. Test full flow. Verify 4/7 ADK claim is honest.
- **R4 eval refresh** (0.5 day): replace dead PDP URLs with live ones (target 15+ eligible). Add synthetic-fixture eval track as a separate scoreboard (5 cases, PDP-text-only, never merged with real-URL track). Re-run.
- **Codex handshake** before EOD.

### Day 22 (2026-05-26) — 1.5 days
- **U5 `/agents` page reorder** (0.5 day): Errors HTTP code table + Rate limits quota table added. Pricing + "We grade. We don't decide." moved to Product page.
- **U6 primary-CTA discipline** (0.5 day): one primary CTA per page. Product = "Try the cached demo." Developers = "Run the curl." Architecture = "View a signed bundle." Demo = existing.
- **R5 honest language pass** (0.5 day): everywhere we say "mesh" implying internal A2A, replace with "single-process multi-agent pipeline (asyncio fan-out), public A2A mesh at the edge." Update `/architecture` + `/agents` + Devpost draft.
- **Codex handshake** before EOD.

### Day 23 (2026-05-27) — 1 day
- **N3b Firestore append UI honesty** (1 hour): when append fails, UI shows "chain anchor unavailable" not "appended."
- **N3c silent-failure audit** (4 hours): every `except Exception` in `agents/` either logs+raises or surfaces in API response.
- **N4 base URL template-inject** (1 hour): `/agents` page uses `PUBLIC_BASE_URL` from server-side template, not hardcoded.
- **N2 max_claims align** (1 hour): eval submits 3 not 1, or UI shows 1 not 3. Pick one and ship both consistent.
- **Codex handshake** before EOD.

### Day 24 (2026-05-28) — 1 day
- **B1 repo public** + README polish for judge-browsing-code (1 day). Move all stale TODOs out of README. Keep architecture diagram + quickstart + Track 3 claims + license.
- **Codex handshake** before EOD.

### Day 25–27 (2026-05-29 → 2026-05-31) — 3 days
- **B2 demo video ≤3 min**. Centerpiece = `/demo/shopper` against rebuilt mesh (4/7 ADK, working RAG, fail-CLOSED adversarial). ElevenLabs founder VO over Lyria bed. Day 25 shoot, Day 26 edit, Day 27 final.

### Day 28 (2026-06-01) — 1 day
- **B3 Devpost ~700w text**. Problem → why now → buyer → multi-agent → why us → ask.

### Day 29 (2026-06-02) — 0.5 day
- 🔒 **DEMO FREEZE**. Codex final sweep. Stranger test with 3 fresh readers.

### Day 30–31 (2026-06-03 → 2026-06-04) — buffer
- Absorb stranger findings.
- Final polish.

### Day 32 (2026-06-05) — submit
- 🚀 **SUBMIT by noon PT.**

---

## Budget summary

| Bucket | Days |
|---|---|
| R1 verification | 0.1 (was 0.5) |
| R2 Reasoning Engine wired | 1.0 |
| R3 ADK 4/7 migration | 2.0 |
| R4 eval refresh + synthetic track | 0.5 |
| R5 honest language pass | 0.5 |
| N1+N3a+N3b+N3c+N4 (silent-failure cleanup + skill wiring) | 1.5 |
| U1–U6 UX rebuild | 3.5 |
| B1 repo public | 1.0 |
| B2 video | 3.0 |
| B3 Devpost | 1.0 |
| Freeze + stranger test | 0.5 |
| Buffer | 1.0 |
| **Total** | **15.6** |

Deadline is 15 days. Over by 0.6 day. Buffer comes from:
- R1 dropping to 0.1 day (corpus already exists)
- Compress U5+U6 into one day (already adjacent)
- Compress R5 into Day 23 silent-failure cleanup day

Final: **15.0 days exactly.** No buffer beyond Day 30/31.

---

## What to do if you lose this session

1. Read `docs/PRODUCTION_REVIEW_2026-05-21.md` first (full audit + codex handshake).
2. Read this file (`docs/RESUME_2026-05-21.md`) second.
3. Read `docs/SUBMISSION_AUDIT_2026-05-21.md` for the morning context.
4. Check `git log --oneline -10` to see what shipped since.
5. Run `gcloud config configurations activate pawconscious-mesh` before any gcloud command (per `feedback_gcloud_per_project_configs`).
6. Verify state: `curl https://mesh-api-oasa5mxega-uc.a.run.app/health/agent-engine` should return the Reasoning Engine resource name.
7. Continue from the Day-X bucket that matches today's date.

---

## Resume command (paste into any new session)

```
pawconscious-resume-21may: Read docs/RESUME_2026-05-21.md and PRODUCTION_REVIEW_2026-05-21.md. Audit repo state per feedback_audit_repo_not_narrative. Confirm Day-X position vs the plan. Continue without re-asking the four decisions Omar locked.
```
